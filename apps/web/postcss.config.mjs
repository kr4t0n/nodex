/** Tailwind v4 is a PostCSS plugin; there is no tailwind.config for the theme,
    which lives in `@theme` inside globals.css instead. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
