import { Details } from './component';

export function Example() {
  return (
    <div className="w-full max-w-[440px]">
      <Details summary="What does close read mean?" meta="Density" open>A close-read chart draws one mark per record and rewards study. If ninety days are being shown, ninety marks are drawn. Use it for reports, not for dashboards.</Details>
      <Details summary="Why are the strokes so thin?" meta="Tokens">Sub-pixel hairlines are the strongest signature in the language. A two pixel line reads as a different product entirely.</Details>
      <Details summary="Can I edit what I add?">Yes. Added components are copies you own. Nothing updates them and there is no version to upgrade.</Details>
    </div>
  );
}
