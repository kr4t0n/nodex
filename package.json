{
  "name": "nodex",
  "version": "0.1.0",
  "private": true,
  "description": "A design-language-first component registry",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:registry": "node scripts/build-registry.mjs",
    "db:up": "docker compose up -d --wait postgres",
    "db:down": "docker compose down",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate.mjs",
    "check:registry": "node scripts/build-registry.mjs --check",
    "check:shell": "node scripts/check-shell-primitives.mjs",
    "smoke": "node scripts/smoke-components.mjs",
    "typecheck": "tsc --build && npm run typecheck --workspace @nodex/web",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "dev": "npm run dev --workspace @nodex/web",
    "smoke:cli": "node scripts/smoke-cli.mjs",
    "build": "npm run build:registry && npm run build --workspace @nodex/web",
    "start": "npm run start --workspace @nodex/web"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.2.0",
    "eslint": "10.9.0",
    "globals": "17.11.0",
    "jiti": "2.7.0",
    "jsdom": "30.0.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0"
  }
}
